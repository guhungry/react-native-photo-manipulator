appId: photomanipulator.example
---
- launchApp

- scrollUntilVisible:
    element:
      id: "example-exampleOptimize"
    direction: DOWN
    visibilityPercentage: 80
    timeout: 30000
    speed: 100
- assertVisible:
    id: "example-exampleOptimize"

- assertVisible:
    id: "optimizeResult"